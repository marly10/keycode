---

  tasks:
  - name: creating localhost file
  - path: /home/ubuntu/hosts
    owner: ubuntu
    content: |
      localhost ansible_connection=local

  - name: Ansible create file if it doesn't exist example
    file:
      path: "/home/ubuntu/this_is_from_github.txt"
      state: touch

...